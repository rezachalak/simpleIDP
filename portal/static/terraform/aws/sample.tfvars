cluster_name    = $CLUSTER_NAME
cluster_version = $CLUSTER_VERSION

vpc_cidr             = $VPC_CIDER_BLOCK
availability_zones   = $AZS
private_subnet_cidrs = $PRIVATE_SUBNETS_CIDERS
public_subnet_cidrs  = $PUBLIC_SUBNETS_CIDERS

min_size     = $MIN_SIZE
max_size     = $MAX_SIZE
desired_size = $DESIRE_SIZE

instance_type = $INSTANCE_TYPE
